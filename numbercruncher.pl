#!/usr/bin/perl


# Checks that the name of a file in directory `{nietvrij,vrij}/12/1234` begins with `1234`
# Checks that said file name also doesn’t contain spaces or “forbidden characters”
# Checks that only PSDs are in the `vrij/` directory but anything can be in `nietvrij/`
# Reports any discrepancies by printing messages to STDOUT


exit;
